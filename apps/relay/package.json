{
  "name": "@agent-relay/relay",
  "version": "0.1.0",
  "description": "relay – CLI that connects a Claude Code executor to the agent-relay gateway via a WebSocket tunnel",
  "type": "module",
  "bin": {
    "relay": "src/index.ts"
  },
  "main": "src/index.ts",
  "scripts": {
    "start": "node --import tsx/esm src/index.ts"
  },
  "dependencies": {
    "@agent-relay/domain": "workspace:*",
    "@anthropic-ai/claude-code": "^2.1.0",
    "commander": "^14.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
